{
  "name": "claudecommit",
  "version": "0.1.0",
  "description": "Generate git commit messages with Claude, using your Claude Code subscription.",
  "main": "index.ts",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "bin": {
    "cc": "bin/cc.ts",
    "claudecommit": "bin/cc.ts"
  },
  "files": [
    "bin",
    "src",
    "index.ts"
  ],
  "devDependencies": {
    "@types/bun": "latest",
    "skilld": "^2.0.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.196",
    "@opentui/core": "^0.4.2",
    "commander": "^15.0.0"
  },
  "scripts": {
    "start": "bun run bin/cc.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "prepare": "skilld prepare || true"
  }
}
